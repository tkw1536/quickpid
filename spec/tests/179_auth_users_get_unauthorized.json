{
  "name": "auth_users_get_unauthorized",
  "comment": "GET /users/ without authentication returns 401 unauthorized.",
  "steps": [
    {
      "name": "list_unauthenticated",
      "config": {},
      "request": {
        "method": "GET",
        "path": "/users/"
      },
      "response": {
        "code": 401,
        "body": {
          "error": "unauthorized"
        }
      }
    }
  ]
}
